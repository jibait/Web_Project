{% extends 'base.html.twig' %}

{% block headerapp %}

<header>
    
    <div class="banner-container">
        <a href="{{path('app_product_index')}}" class="nav-links mr-0"><img src="{{asset('Medias/WareSaurus.png')}}" alt="Image" class="banniere-image"></a>
        <div class="banner-search">
          <input type="text" class="search-input" placeholder="Search">
          <button class="search-button">Search</button>
        </div>
        <div class="banner-text">
          {% if app.user %}
            <a href="{{path('app_log_out')}}" class="nav-links">Log out</a>
          {% else %}
            <a href="{{path('app_login')}}" class="nav-links">Log in</a>
          {% endif %}
          <a class="nav-links" href="{{path('app_product_index')}}">Home</a>
          <a class="nav-links" href="{{path('app_product_new')}}">Add a new product</a>
          <a href="{{path('app_product_cart')}}" class="nav-links mr-0">My cart </a>
        </div>
        <a href="{{path('app_product_cart')}}" class="nav-links mr-0"><img src="{{asset('Medias/panier.png')}}" alt="Image" class="search-image"></a>
    </div>
</header>

<div class="banner-container-login">
    <div class="banner-text-login">
      <h1>IT</h1>
      <h1>Plushies</h1>
      <h1>School Supplies</h1>
      <h1>Decoration & Furnitures</h1>
      <h1>Books</h1>
      <h1>Cosplay</h1>
      <h1>Glasses</h1>
      <h1>More</h1>
      <div class="oval">FR</div>
      <div class="oval">EN</div>
    </div>
  </div>

{% endblock %}
